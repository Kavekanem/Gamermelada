using System.Collections;
using System.Collections.Generic;
using UnityEngine;


public class ParticleMovement : MonoBehaviour
{
    public GameObject Player;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //this.gameObject.transform.rotation = new Quaternion(1.0f, this.gameObject.transform.rotation.x, this.gameObject.transform.rotation.y, Player.transform.rotation.y);
        //this.gameObject.transform.rotation = Quaternion.Euler(90.0f, this.gameObject.transform.rotation.y, -90.0f);
        //this.gameObject.transform.rotation = Player.transform.rotation;
    }
}
